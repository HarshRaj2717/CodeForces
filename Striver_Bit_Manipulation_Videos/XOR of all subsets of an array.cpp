// Problem : Find XOR of all subsets of an array
#include <bits/stdc++.h>

using namespace std;

int solver(vector<int> nums)
{
    // Write all the subsets aur [1, 2, 3] and you will see it!
    return 0;
}

int main()
{
    int n;
    cin >> n;
    vector<int> nums(n);
    for (int i = 0; i < n; i++)
        cin >> nums[i];

    cout << solver(nums) << endl;

    return 0;
}